# dskcode — 自定义指令

## 主题配色

本项目的终端 UI 主题色定义如下，所有新增代码请遵循此调色板：

| 用途 | 色值 | 说明 |
|---|---|---|
| **主题色 / 边框** | `#5686fe` | 输入框上下边框、模型名称高亮 |
| 用户消息竖线 | `#FF8C00` | 用户消息左侧标识条 |
| 助手消息竖线 | `#E040FB` | 助手消息左侧标识条 |
| 输入 prompt 绿 | `#00ff41` | 高亮/选中状态 |
| 蓝色高亮 | `#00bfff` | 命令/skill 列表高亮 |
| 次级文本 | `#808080` | 描述、辅助信息 |
| 费用色 | `#ffd700` | 金额显示 |
| 错误色 | `#ff6b6b` | 错误信息 |
| 警告色 | `#ffa500` | 警告提示 |

### 在代码中使用

主题色定义在 `src/ui/tui/theme.ts` 的 `COLORS` 对象中。ANSI 转义序列统一使用内联的 `\x1b[38;2;R;G;Bm` 格式（24-bit 真彩色），不依赖 chalk 等包装库。

示例：
```typescript
// 主题色（#5686fe）
`\x1b[38;2;86;134;254m${text}\x1b[0m`
```
